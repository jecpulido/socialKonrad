<%= form_with(model: correo, local: true) do |form| %>
  <% if correo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(correo.errors.count, "error") %> prohibited this correo from being saved:</h2>

      <ul>
      <% correo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :correo %>
    <%= form.text_field :correo %>
  </div>

  <div class="field">
    <%= form.label :Usuario_id %>
    <%= form.text_field :Usuario_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
