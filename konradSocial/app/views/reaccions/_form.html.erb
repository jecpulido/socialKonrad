<%= form_with(model: reaccion, local: true) do |form| %>
  <% if reaccion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reaccion.errors.count, "error") %> prohibited this reaccion from being saved:</h2>

      <ul>
      <% reaccion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Entrada_id %>
    <%= form.text_field :Entrada_id %>
  </div>

  <div class="field">
    <%= form.label :Usuario_id %>
    <%= form.text_field :Usuario_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
